{
  "name": "tyshSpy-Message-Generator",
  "version": "1.0.0",
  "description": "Generate a discord message that looks like tyshSpy's saying",
  "main": "index.js",
  "author": "dragonyc1002 / kyometori",
  "license": "",
  "homepage": "https://dragonyc1002.github.io/tyshSpygen",
  "dependencies": {
    "@skyra/discord-components-core": "^3.4.1",
    "@skyra/discord-components-react": "^3.4.1",
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "gh-pages": "^3.2.3",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "web-vitals": "^1.0.1",
    "yarn": "^1.22.11"
  },
  "scripts": {
    "start": "react-scripts start --openssl-legacy-provider start",
    "build": "react-scripts build --openssl-legacy-provider build",
    "test": "react-scripts test --openssl-legacy-provider test",
    "eject": "react-scripts eject --openssl-legacy-provider eject",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not ie <= 10",
      "not ie_mob <= 10",
      "not bb <= 10",
      "not op_mob <= 12.1",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
